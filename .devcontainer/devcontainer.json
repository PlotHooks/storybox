{
  "name": "Storybox Laravel",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "20"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y git-lfs sqlite3 && git lfs install && touch database/database.sqlite && composer install && bash scripts/setup-codex.sh",
  "forwardPorts": [8000]
}